<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="TOL2.mapper.PageMapper" >

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, userId, name, category, num, price, pictures, likeNum, isSold, description
    </sql>
    <!-- 分页SQL语句 -->
    <!-- <select id="selectAllGoodsByPage" resultMap="TOL2.model.Goods">resultMap错的 找不到-->
    <select id="selectAllGoodsByPage" parameterType="java.lang.Integer" resultType="TOL2.model.Goods">
        select
        <include refid="Base_Column_List" />
        from goods LIMIT #{startPos},#{pageSize}
    </select>

    <select id="selectAllGoodsByPageAndUserId" parameterType="java.lang.Integer" resultType="TOL2.model.Goods">
        select
        <include refid="Base_Column_List" />
        from goods WHERE userId = #{userId,jdbcType=INTEGER} LIMIT #{startPos},#{pageSize}
    </select>
    <!-- 取得记录的总数 -->
    <select id="getAllGoodsCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM goods
    </select>

    <select id="getGoodsCountByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM goods WHERE userid =#{userId,jdbcType=INTEGER}
    </select>
</mapper>
